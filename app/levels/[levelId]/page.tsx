import Link from "next/link";
import { level } from "@/lib/content";
export default async function LevelPage({ params }: { params: Promise<{ levelId: string }> }) { const { levelId } = await params; if (levelId !== level.id) return <div className="page"><h1>Level not found</h1></div>; return <div className="page"><div className="content-narrow"><p className="eyebrow">Your starting point</p><h1>{level.title}</h1><p className="lede">{level.description}</p></div><section className="section" style={{ marginTop: 90 }}><div className="section-heading"><h2>Choose a thread</h2><p>Follow the order that suits you. Nothing is locked.</p></div><div className="row-list">{level.categories.map((category, index) => <Link className="list-row" href={`/levels/${level.id}/categories/${category.id}`} key={category.id}><span className="list-index">0{index + 1}</span><span><h3>{category.title}</h3><p>{category.description} · {category.modules.length} lessons</p></span><span className="text-link">View lessons →</span></Link>)}</div></section></div>; }
