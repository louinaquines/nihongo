import Link from "next/link";
import { level } from "@/lib/content";

export default function Home() { return <div className="page"><section className="hero"><p className="eyebrow">A quieter way to learn Japanese</p><h1>Make space for your first words.</h1><p className="lede">A calm, structured path through kana, vocabulary, and grammar. Start with one small lesson, then put it to work.</p><p style={{ marginTop: 35 }}><Link className="button" href={`/levels/${level.id}`}>Begin the N5 path</Link></p></section><section className="section"><div className="section-heading"><h2>Learn → Quiz → Level up</h2><p>Short lessons, immediate practice, and progress that stays on your device.</p></div><div className="row-list">{level.categories.map((category, index) => <Link className="list-row" href={`/levels/${level.id}/categories/${category.id}`} key={category.id}><span className="list-index">0{index + 1}</span><span><h3>{category.title}</h3><p>{category.description}</p></span><span className="text-link">Explore →</span></Link>)}</div></section></div>; }
